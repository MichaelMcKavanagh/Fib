# Fib
Fibonacci game

## Requirements

This project assumes you have nodeJS installed.

## Getting started

1. install: `npm install`

2. test: `npm run test`

3. build: `npm run build`

4. run: `npm run start`
